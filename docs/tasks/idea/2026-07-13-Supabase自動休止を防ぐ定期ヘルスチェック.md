# アイデア: Supabase 無料枠の自動休止を防ぐ定期ヘルスチェック

- 日付: 2026-07-13
- 種別: idea / ops（仕様・実装方針はスコープ外。着手時に別タスクへ切り出す）
- マスター: **このドキュメントを正本とする。** lumiere-select 側の同名タスクは重複防止のためこちらを参照する（理由は後述「共有プロジェクト」）。
- 状態: **2026-07-14 実装・本番反映完了。** 実装・リリース証跡は `docs/tasks/done/2026-07-14-supabase-keepalive-cron.md`、運用正本は `docs/runbooks/supabase-keepalive.md`。

## 実装結果（2026-07-14）

- 実行場所: Cloudflare Cron Worker `shared-supabase-keepalive`（このリポジトリの1系統のみ）
- schedule: 毎日 03:23 UTC
- activity: `project_health` singleton row の PostgREST SELECT を3回
- secret: Cloudflare Worker Secret `SUPABASE_ANON_KEY`
- observability: Workers Logs + Cron Events。失敗時は scheduled handler が throw
- Edge Function OPTIONS 案は DB query の根拠がないため不採用
- GitHub Actions 案は public repository が60日無活動だと schedule 自動停止になるため不採用
- 本番 migration / secret / Worker deploy / Cron Trigger 登録は 2026-07-14 に完了

## 発端

2026-07-13、AI 相談機能が全応答エラーになっていた。原因はコードではなく、**Supabase プロジェクト `ftimimljrflfboopsqgm` が無料枠の「7 日間無アクセス」で自動休止(INACTIVE)** し、subdomain が DNS から外れて全 Edge Function 呼び出しが失敗パスに落ちていたこと。Management API の restore で復帰済み。

## 背景 / なぜ必要か

- Supabase 無料枠は **7 日間プロジェクトへのアクセスが無いと自動 pause** する仕様。
- 個人ポートフォリオは実トラフィックが少なく、無アクセス期間が 7 日を超えやすい → **再発リスクが構造的に高い**。
- pause されると AI 機能（Edge Function）が全滅し、気づくまで放置される。

## ゴール

- プロジェクトを定期的にアクティブへ保ち、**無料枠のまま**自動休止を防ぐ。
- 併せて、万一止まっても早期検知できる状態にする（keep-alive 兼 死活監視）。

## 重要 — この Supabase プロジェクトは 2 サイトで共有

- `kurosawa-ai-consulting-site` と `lumiere-select` は **同一プロジェクト `ftimimljrflfboopsqgm`** を使用（両リポの `env/config.yaml` が同じ URL を指す）。
- → keep-alive は **1 系統に集約**する。2 つ作らない（同じプロジェクトを二重に叩くだけで無駄・drift の温床）。
- cron をどちらのリポに置くかは着手時に 1 箇所へ決定し、他方はそれを参照する。

## 決定事項

- Cloudflare Cron Worker を毎日 03:23 UTC に実行する。
- `project_health` singleton row を PostgREST 経由で3回 SELECT し、確実に user database activity を発生させる。
- Worker の `workers.dev` と Preview URL は無効にし、Cron Trigger だけを入力とする。
- 失敗は Workers Logs / Cron Events へ記録し、例外を throw して失敗扱いにする。

## 着手時に確定すべきオープン事項

- [x] Edge Function ではなく極小 `project_health` table の PostgREST SELECT を採用。
- [x] インターバルは毎日。1回の scheduled event で3 query。
- [x] 実行場所は Cloudflare Cron Worker。
- [x] 失敗は Cron Event と Workers Logs に記録。外部 Slack / email integration は追加しない。
- [x] anon key は Cloudflare Worker Secret。ハードコードしない。

## スコープ外

- アプリコードの変更（今回の復旧でコードは無傷と確認済み）。
- 有料プラン移行（pause を完全に無くす別解。コスト方針は別途判断）。

## 参考（今回の一次情報）

- 復帰手順: Management API `POST /v1/projects/ftimimljrflfboopsqgm/restore`（CLI のアクセストークンで実行、HTTP 200）→ `INACTIVE → COMING_UP → RESTORING → ACTIVE_HEALTHY`。
- 対象 Edge Function: kuro = `consult-engineer` / lumiere = `concierge`, `gift-message`, `recommend-products`。
- 復帰後 E2E: `consult-engineer` に実相談を投げ、`fit: "high"` の正常 JSON を確認済み。
